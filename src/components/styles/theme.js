import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#be8f2d",
    },
    secondary: {
      main: "#ff4400",
    },
  },

  typography: {
    // htmlFontSize: 10,
    fontFamily: "Poppins",
    // fontSize: "2rem"
  },
});
